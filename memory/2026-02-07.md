# 2026-02-07 - Saturday

## Morning Chaos & Recovery

Nick had a rough morning:
- **Display manager broke** on Ubuntu — required reinstall to get back in
- **Ollama integration attempt** went pear-shaped when trying to set it up for heartbeat
- ChatGPT helped recover by removing Ollama config from `openclaw.json`

### Gateway Key Confusion
- Old key `7c66def5...` was cached in browser localStorage
- New key `e7d4072832b32a2e75650d375b27372c555783120ba71a96` was in config
- Fixed by clearing Firefox localStorage (Storage tab in dev tools) and re-entering new key

### Lessons Learned
- Firefox: dev tools use "Storage" tab, not "Application" (that's Chrome)
- Private browser won't persist webchat credentials
- Gateway restart doesn't help if the browser is caching the old key

## Afternoon: Ollama Round 2

Attempted to set up Ollama for heartbeats again (based on a video transcript Nick shared about cost optimization).

**What we did:**
- Created `MODEL_SELECTION_RULES.md` with model escalation guidance
- Added explicit Ollama provider config with llama3.2:3b and qwen2.5:3b
- Set `heartbeat.model: "ollama/llama3.2:3b"`

**Current state:**
- Config looks correct (verified in openclaw.json)
- `openclaw models list` doesn't show Ollama models (might be by design for custom providers)
- Haven't verified heartbeats actually run on Ollama yet — set reminder for tomorrow 10:00

**Key learning for Nick:**
- Main session model (Opus) and heartbeat model (Ollama) are separate
- MODEL_SELECTION_RULES.md is behavioral guidance, not automatic switching
- Gateway restart reloads config but doesn't change mid-conversation model

**TODO tomorrow:**
- Check logs for `provider=ollama` heartbeat entries
- Discuss calendar integration

## Status
- Telegram: ✅ working
- Webchat: ✅ working
- Ollama heartbeats: ⏳ configured, pending verification
